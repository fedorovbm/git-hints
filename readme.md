\# 📌 Шпаргалка по базовым командам Git



Краткий справочник по основным командам системы контроля версий Git.



---



\## 📁 Инициализация и клонирование



| Команда | Описание |

|--------|----------|

| `git init` | Инициализирует новый репозиторий Git в текущей директории |

| `git clone <url>` | Клонирует удалённый репозиторий на локальный компьютер |



---



\## 📤 Работа с изменениями



| Команда | Описание |

|--------|----------|

| `git status` | Показывает текущее состояние рабочей директории и индекса |

| `git add <file>` | Добавляет файл в индекс (staging area) |

| `git add .` | Добавляет все изменения в текущей директории в индекс |

| `git restore --staged <file>` | Убирает файл из индекса, но оставляет изменения в рабочей директории |

| `git restore <file>` | Отменяет изменения в рабочей директории (до коммита) |



---



\## 💾 Создание коммитов



| Команда | Описание |

|--------|----------|

| `git commit -m "сообщение"` | Создаёт коммит с указанным сообщением |

| `git commit -a -m "сообщение"` | Добавляет и коммитит отслеживаемые файлы без `git add` |

| `git log` | Показывает историю коммитов |

| `git log --oneline` | Краткий вывод истории коммитов |

| `git show <commit>` | Показывает изменения, внесённые конкретным коммитом |



---



\## 🌿 Ветки



| Команда | Описание |

|--------|----------|

| `git branch` | Показывает список веток |

| `git branch <name>` | Создаёт новую ветку |

| `git checkout <branch>` | Переключается на указанную ветку |

| `git switch <branch>` | Альтернатива `checkout` для переключения веток (новый синтаксис) |

| `git merge <branch>` | Сливает указанную ветку в текущую |

| `git branch -d <name>` | Удаляет ветку |



---



\## 🔄 Работа с удалённым репозиторием



| Команда | Описание |

|--------|----------|

| `git remote -v` | Показывает список удалённых репозиториев |

| `git fetch` | Загружает изменения из удалённого репозитория (без слияния) |

| `git pull` | Выполняет `fetch` + `merge` (обновляет текущую ветку) |

| `git push` | Отправляет локальные коммиты в удалённый репозиторий |

| `git push origin <branch>` | Отправляет конкретную ветку на удалённый репозиторий |



---



\## 🚨 Отмена изменений



| Команда | Описание |

|--------|----------|

| `git reset --soft <commit>` | Отменяет коммиты, но сохраняет изменения в индексе |

| `git reset --hard <commit>` | Полностью отменяет изменения (осторожно!) |

| `git revert <commit>` | Создаёт новый коммит, отменяющий указанный |



---



> 💡 \*\*Совет:\*\* Всегда делайте `git status` и `git log` для понимания текущего состояния репозитория.



